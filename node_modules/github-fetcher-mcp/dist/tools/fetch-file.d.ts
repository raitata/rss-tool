import { z } from 'zod';
import type { RepoIdentifier } from '../types/index.js';
import type { GitHubFetcher } from '../services/github-fetcher.js';
/**
 * Creates the fetch-file tool
 * @param fetcher - GitHub fetcher service
 * @param repoIdentifier - Optional repository identifier from CLI args
 */
export declare function createFetchFileTool(fetcher: GitHubFetcher, repoIdentifier?: RepoIdentifier): {
    name: string;
    description: string;
    inputSchema: z.ZodObject<{
        filePath: z.ZodString;
    }, "strip", z.ZodTypeAny, {
        filePath: string;
    }, {
        filePath: string;
    }>;
    handler(input: {
        filePath: string;
    }): Promise<{
        content: {
            type: "text";
            text: string;
        }[];
    }>;
};
//# sourceMappingURL=fetch-file.d.ts.map