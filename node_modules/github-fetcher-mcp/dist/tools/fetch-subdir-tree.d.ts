import { z } from 'zod';
import type { RepoIdentifier } from '../types/index.js';
import type { GitHubFetcher } from '../services/github-fetcher.js';
/**
 * Creates the fetch-subdir-tree tool
 * @param fetcher - GitHub fetcher service
 * @param repoIdentifier - Optional repository identifier from CLI args
 */
export declare function createFetchSubdirTreeTool(fetcher: GitHubFetcher, repoIdentifier?: RepoIdentifier): {
    name: string;
    description: string;
    inputSchema: z.ZodObject<{
        dirPath: z.ZodString;
    }, "strip", z.ZodTypeAny, {
        dirPath: string;
    }, {
        dirPath: string;
    }>;
    handler(input: {
        dirPath: string;
    }): Promise<{
        content: {
            type: "text";
            text: string;
        }[];
    }>;
};
//# sourceMappingURL=fetch-subdir-tree.d.ts.map